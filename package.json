{
  "name": "AnimationOnScroll",
  "license": "MIT",
  "scripts": {
    "start": "open 'index.html' && babel --watch=./app --out-dir=./dist",
    "dev": "babel --watch=./app --out-dir=./dist"
  },
  "babel": {
    "plugins": [
      "transform-flow-strip-types"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "flow-bin": "^0.41.0"
  }
}
